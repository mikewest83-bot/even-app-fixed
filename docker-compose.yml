services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: pg
      POSTGRES_DB: even
    ports:
      - "5432:5432"
    volumes:
      - even_pg:/var/lib/postgresql/data
volumes:
  even_pg:
